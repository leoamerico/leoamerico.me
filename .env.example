# .env.example — leoamerico.me
# ════════════════════════════════════════════════════════════════
# Env Neo Ltda · CNPJ 36.207.211/0001-47
# Template de variáveis de ambiente. Nunca commitado com valores reais.
# Copie como .env.local para desenvolvimento; configure em Vercel para prod.
# ════════════════════════════════════════════════════════════════
#
# ENFORCEMENT: audit:infra I6 verifica que este arquivo existe.
#              audit:infra I7 verifica que nenhum valor real foi
#              commitado no source. Gate S4 (ESA) cobre /ceo/** vars.
#
# GERAÇÃO das variáveis CEO:
#   CEO_PASSPHRASE_HASH:
#     $ node -e "const b=require('bcryptjs');b.hash('SUA_FRASE',12,(_,h)=>console.log(h))"
#   CEO_TOTP_SEED (base32, 32 chars):
#     $ node -e "const c=require('crypto');console.log(c.randomBytes(20).toString('base64').replace(/[^A-Z2-7]/gi,'').toUpperCase().slice(0,32))"
# ════════════════════════════════════════════════════════════════

# ── GitHub API (audit ao vivo — entityCount, commits) ────────────
GITHUB_TOKEN=ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ── CEO / ESA (Arquitetura de Segurança Empresarial) ─────────────
# Gerado localmente. Nunca o texto plano. Nunca em Git.
CEO_PASSPHRASE_HASH=<bcrypt-hash-gerado-localmente>

# Seed TOTP base32 (32 chars). Registrar no autenticador antes de ativar.
CEO_TOTP_SEED=<base32-seed-gerado-localmente>

# Senha de criptografia do Iron Session (mínimo 32 chars)
CEO_SESSION_SECRET=<aleatorio-32-chars-minimo>

# ── Ambiente ─────────────────────────────────────────────────────
# Preenchido automaticamente pelo Vercel. Não definir manualmente.
# VERCEL_ENV=production | preview | development
# NEXT_PUBLIC_VERCEL_URL=<automatico>
